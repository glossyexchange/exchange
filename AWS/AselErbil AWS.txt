AWS account
aselerbilaws@gmail.com
pass: Asel@aws@2025

-----------------

cd AWS folder


ssh -i "Asel Erbil.pem" ubuntu@ec2-3-87-82-224.compute-1.amazonaws.com
sudo apt-get update

curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs

sudo apt-get install nginx
sudo service nginx start
sudo service nginx status

Nginx Configuration
cd /etc/nginx/
sudo vi nginx.conf

/ to admission permitasion
sudo vi nginx.conf



/etc/nginx/nginx.conf 

user ubuntu;
worker_processes  1;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    client_body_buffer_size 256M;
    client_header_buffer_size 256M;
    client_max_body_size 256M;
    large_client_header_buffers 2 256M;
    client_body_timeout 10;
    client_header_timeout 10;
    keepalive_timeout 5 5;
    send_timeout 10;
    server_tokens off;
    #gzip  on; on;

    include /etc/nginx/conf.d/*.conf;
}


/* :wq to exit

go to 
cd conf.d/
sudo touch default.conf

sudo vi default.conf



cd /etc/nginx/default.conf


server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name  3.90.104.163;

    # Frontend Configuration
    access_log /home/ubuntu/client/server_logs/host.access.log main;

    location / {
        root /home/ubuntu/client/deploy;
        index index.html index.htm;
        try_files $uri /index.html;
        
        add_header X-Frame-Options SAMEORIGIN;
        add_header X-Content-Type-Options nosniff;
        add_header X-XSS-Protection "1; mode=block";
        add_header Strict-Transport-Security "max-age=31536000; includeSubdomains;";
    }

    # Admin Configuration
    access_log /home/ubuntu/admin/server_logs/admin.access.log main;

    location /admin {
        alias /home/ubuntu/admin/deploy/;
        index index.html index.htm;
        try_files $uri /index.html;
        
        add_header X-Frame-Options SAMEORIGIN;
        add_header X-Content-Type-Options nosniff;
        add_header X-XSS-Protection "1; mode=block";
        add_header Strict-Transport-Security "max-age=31536000; includeSubdomains;";
    }

    # API Proxy Configuration
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }

    server_tokens off;

    location ~ /\.ht {
        deny all;
    }
}
:qa!
sudo service nginx restart

sudo service nginx status


cd /home/ubuntu/server/
ls

sudo apt install npm
npm install

sudo npm install pm2@latest -g


pm2 start npm --name online-store -- run dev

pm2 start server.js --name online-store

pm2 start server.ts --name "API"

pm2 show API

pm2 delete API 
pm2 start server.ts --name "API" -o ./api.log

 pm2 start server.ts --name "API" -o ./api.log -e ./api.logs --merge-logs

pm2 startup
pm2 save

pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M



//postgrase
sudo apt install postgresql postgresql-contrib

sudo systemctl start postgresql
sudo systemctl enable postgresql

sudo -i -u postgres


pm2 stop online-store
pm2 delete online-store
pm2 start dist/server.ts --name online-store



CREATE DATABASE "onlineStore";
CREATE ROLE admin WITH LOGIN PASSWORD 'admin%40123';
GRANT ALL PRIVILEGES ON DATABASE "onlineStore" TO admin;

DATABASE_URL='postgresql://admin:admin%40123@localhost:5432/onlineStore'

// secure domain

sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d asel.glossycode.com -d www.asel.glossycode.com
sudo certbot --nginx -d api.glossycode.com

sudo systemctl status certbot.timer
sudo certbot renew --dry-run



sudo ufw allow ssh   
sudo ufw allow 'Nginx Full'  
sudo ufw enable




/etc/nginx/conf.d/default.conf





server {
  listen 80;

  location / {
        root /var/www/gulkana;
        index  index.html index.htm;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        try_files $uri $uri/ /index.html;
  }
}


http://3.90.104.163/api/category-get

curl http://localhost:5000/api/category-get



server {
    listen 80;
    server_name asel.glossycode.com www.asel.glossycode.com;

   

    location / {
        root /home/ubuntu/client/deploy;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 80;
    server_name api.glossycode.com;

location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
    allow all;
}

    # Proxy API requests
    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Serve uploads
    location /uploads/ {
        alias /home/ubuntu/server/uploads/;
        try_files $uri $uri/ =404;
    }
}

server {
  listen 80;
  server_name admin.365promo.net;
  location / {
    root /home/ubuntu/admin/deploy;

    index  index.html index.htm;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
    try_files $uri $uri/ /index.html;
  }
}



sudo chmod -R 755 /home/ubuntu/server/uploadss
sudo chown -R www-data:www-data /home/ubuntu/server/uploads


echo "test" | sudo tee /var/www/certbot/test.txt
curl https://api.glossycode.com/.well-known/acme-challenge/test.txt



curl -X POST https://api.glossycode.com/api/v1/admin-login \
  -H "Origin: https://asel.glossycode.com" \
  -H "Content-Type: application/json" \
  -d '{"phone":"4493156","password":"1234567"}' \
  -v

sudo journalctl -u your-node-service -f
# OR if running via pm2:
pm2 logs
# OR if running directly:
node dist/server.ts


ALTER USER admin WITH PASSWORD 'admin@123';

npx prisma migrate deploy



Full backup to a file
pg_dump -U admin -d onlineStore -F c -f /home/ubuntu/onlineStore.backup


//Full backup to a file by day name

pg_dump -U admin -d onlineStore -F c -f /home/ubuntu/${WEEKDAY}_$(date +%Y-%m-%d).backup

//edit crontab
crontab -e

//backup every day at 2:30 AM:
30 2 * * * /home/ubuntu/backup_weekday.sh

//Make sure your script is executable
chmod +x /home/ubuntu/backup_weekday.sh


//Option 2: Backup Directly to Google Drive Using Rclone
-Install rclone on your EC2 instance or local machine.
curl https://rclone.org/install.sh | sudo bash

-Configure Google Drive in rclone:
rclone config

-Choose n for new remote.
Name it gdrive.
Select Google Drive and follow OAuth steps.

-Dump PostgreSQL and send it to Google Drive:
pg_dump -h your-db-endpoint -U your-username -d your-database > backup.sql
rclone copy backup.sql gdrive:/PostgresBackups/



Plain SQL backup (easier to read)
pg_dump -U admin -d onlineStore > /home/ubuntu/onlineStore.sql


Restore Backup
pg_restore -U admin -d onlineStore /home/ubuntu/onlineStore.backup


From plain SQL file
psql -U admin -d onlineStore -f /home/ubuntu/onlineStore.sql



info_db_user
CDS3PNKoySnMuF5x
MongoDb
info@snusconicotine.com
snusco
Snusco@2025



